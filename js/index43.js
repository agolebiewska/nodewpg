const text = 'game of thrones'
const firstChar = text.charAt(0);
const secondChar = text.charAt(5);
const thirdChar = text.charAt(8);
const name = (firstChar.concat (secondChar).concat (thirdChar)); 

console.log(name.toUpperCase());


