for (let number = 1000; number >= 0; number --) {
    if (number = number -= 9);  {
        if (number >= 0 || number == 1000) {
        console.log (number);
    } 
    else {
        console.log(`** ${number} **`);
    }
}
}